# Notes stay human-readable outside Cursor

Search strategy notes under `notes/beauveria-bassiana/` are Obsidian-first: readable without Cursor. Agent policy (ADRs, `.cursor` rules/skills, `temp/` scratch) must not be cited or embedded in note text. Agents may edit notes only when the user explicitly allows. Chosen so scientific search documentation stays usable in Obsidian alone while agent constraints live in rules/ADRs/`CONTEXT.md`.
