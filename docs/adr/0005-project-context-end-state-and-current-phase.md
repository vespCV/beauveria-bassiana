# Project-context keeps end-state and current phase

`.cursor/rules/project-context.mdc` retains the review **Done when** end-state (data extraction ready, populated PRISMA flow, finalized README) and adds a separate **Current phase** block for active work. While pre-gate, Current phase is search documentation. Chosen so agents neither redefine project completion nor treat Phase 2 filing as current.
