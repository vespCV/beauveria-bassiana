# Canonical screening category ids are 01–06

Disk folders under `pdfs/` and the `@file-pdfs` skill define canonical category ids (`01-existing-products` through `06-background-proxies`, including `02a`/`02b`/`02c`). Zero-based labels such as `00-existing-products` in notes should be retitled to match; agent-facing docs must not keep a parallel 00–05 scheme. Chosen to avoid rename churn on the category tree and filing skill.
