# research skill for side questions and pillar-01 lookups

`@research` answers focused questions from primary sources into `temp/research/`. It may cover tooling/docs and pillar `01-existing-products` registry/product/patent lookups. It must not replace systematic database runs, hit-count logging, or Rayyan dedup recorded in notes. Chosen so agents can help with authoritative product sources without forking the PRISMA search record.
